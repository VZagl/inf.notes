# Настройки файла AGENT.md

## 📋 Содержание

1. [Введение в AGENT.md](#введение-в-agentmd)
2. [Назначение и принципы](#назначение-и-принципы)
3. [Структура файла](#структура-файла)
4. [Правила и конвенции](#правила-и-конвенции)
5. [Команды проекта](#команды-проекта)
6. [Настройки среды разработки](#настройки-среды-разработки)
7. [Кодовые соглашения](#кодовые-соглашения)
8. [Технологический стек](#технологический-стек)
9. [Тестирование](#тестирование)
10. [Структура проекта](#структура-проекта)
11. [Продвинутые настройки](#продвинутые-настройки)
12. [Лучшие практики](#лучшие-практики)

---

## Введение в AGENT.md

### Что такое AGENT.md?

**AGENT.md** — это специальный конфигурационный файл для AI помощника Amp (Sourcegraph), который:

- **Настраивает поведение агента** в контексте конкретного проекта
- **Определяет правила работы** и стандарты кодирования
- **Содержит команды** для сборки, тестирования и разработки
- **Указывает технологии** и инструменты проекта
- **Автоматически подключается** к контексту при работе с проектом

### Расположение

- **Корень проекта**: `./AGENT.md`
- **Планируется**: Поддержка локальных файлов в подкаталогах
- **Автоматическое обнаружение**: Amp предложит создать файл, если его нет

---

## Назначение и принципы

### Основные цели

1. **Контекстуализация**: Адаптация поведения ИИ под конкретный проект
2. **Стандартизация**: Единообразие в стиле кода и подходах
3. **Автоматизация**: Знание команд сборки и тестирования
4. **Эффективность**: Сокращение времени на объяснения контекста

### Принципы работы

- **Автоматическое чтение**: Файл автоматически добавляется в контекст
- **Приоритет настроек**: Локальные правила важнее глобальных
- **Версионный контроль**: Файл должен быть в Git для команды
- **Документирование**: Явное описание всех важных аспектов проекта

---

## Структура файла

### Рекомендуемые разделы

```markdown
# AGENT.md - Настройки для AI помощника

## Правила работы

### Язык общения

### Стиль кодирования

### Подтверждения изменений

## Команды проекта

### Разработка

### Сборка и тестирование

### Деплой

## Конвенции именования

### Переменные и функции

### Файлы и папки

### Git коммиты

## Технологии

### Основной стек

### Инструменты разработки

### Зависимости

## Структура проекта

### Организация файлов

### Важные директории

### Конфигурационные файлы
```

### Базовый шаблон

````markdown
# AGENT.md - Настройки проекта

## Правила работы

### Язык общения

- Основной язык проекта: [русский/английский]
- Комментарии в коде: [русский/английский]
- Документация: [русский/английский]

### Подтверждение изменений

- Показывать план перед изменениями: [да/нет]
- Запрашивать подтверждение: [всегда/только для критичных]

## Команды проекта

```bash
# Разработка
npm run dev
npm run build
npm run test
npm run lint
```
````

## Технологии

- Основной язык: JavaScript/TypeScript/Python/etc
- Фреймворк: React/Vue/Angular/etc
- Инструменты: Webpack/Vite/etc

## Структура проекта

- `/src/` - исходный код
- `/tests/` - тесты
- `/docs/` - документация

````

---

## Правила и конвенции

### Языковые настройки

#### Выбор языка общения
```markdown
### Язык общения
- **ВСЕГДА отвечать на русском языке**
- Объяснять логику кода на русском языке
- Комментарии к коду писать на русском
- Названия переменных/функций оставлять на английском
- Сообщения об ошибках переводить и объяснять на русском
- Использовать примеры кода с русскими комментариями
- При рефакторинге объяснять причины изменений на русском
````

#### Многоязычные проекты

```markdown
### Языковые настройки

- Интерфейс: русский
- Код и комментарии: английский
- Документация: русский
- API документация: английский
- Git сообщения: русский
```

### Стиль кодирования

#### Форматирование

```markdown
### Стиль кода

- Отступы: 2 пробела / 4 пробела / табы
- Кавычки: одинарные / двойные
- Точки с запятой: обязательно / опционально
- Длина строки: 80 / 100 / 120 символов
- Завершающие запятые: да / нет
```

#### Именование

```markdown
### Базовые конвенции именования

- Переменные: camelCase / snake_case (зависит от языка)
- Константы: UPPER_CASE
- Функции: camelCase / PascalCase (зависит от языка)
- Классы: PascalCase
- Компоненты: PascalCase

> Подробные правила см. в разделе "Кодовые соглашения"

### Именование файлов

- **Обычные файлы: kebab-case** (например: `user-service.ts`, `weather-api.ts`)
  - Совместимость с ОС и URL
  - Лучшая читаемость
  - Стандарт веб-экосистемы
- **React компоненты: PascalCase** (например: `UserProfile.tsx`, `WeatherCard.tsx`)
  - Соответствует именам компонентов в коде
  - Стандарт React экосистемы
```

### Подтверждение изменений

```markdown
### Подтверждение изменений

- **ВСЕГДА показывать план изменений перед edit_file/create_file**
- Описать что будет изменено/создано перед применением
- Дождаться явного подтверждения от пользователя
- Для критичных файлов требовать двойное подтверждение
```

---

## Команды проекта

### Основные команды разработки

#### Node.js проекты

````markdown
### Команды разработки

```bash
# Установка зависимостей
npm install / pnpm install / yarn install

# Запуск в режиме разработки
npm run dev / pnpm run dev / yarn dev

# Сборка проекта
npm run build / pnpm run build / yarn build

# Проверка типов (TypeScript)
npm run typecheck / pnpm run typecheck

# Линтинг
npm run lint / pnpm run lint
npm run lint:fix / pnpm run lint:fix

# Форматирование
npm run format / pnpm run format

# Тестирование
npm test / pnpm test / yarn test
npm run test:watch / pnpm run test:watch
npm run test:coverage / pnpm run test:coverage
```
````

````

#### Python проекты
```markdown
### Команды разработки

```bash
# Создание виртуального окружения
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

# Установка зависимостей
pip install -r requirements.txt
pip install -r requirements-dev.txt

# Запуск приложения
python main.py
python -m app

# Тестирование
pytest
pytest --cov=src tests/

# Линтинг
flake8 src/
black src/
isort src/

# Проверка типов
mypy src/
````

````

#### Другие технологии
```markdown
### Команды (Go)
```bash
go build
go test ./...
go run main.go
go mod tidy
````

### Команды (Rust)

```bash
cargo build
cargo test
cargo run
cargo check
cargo clippy
cargo fmt
```

### Команды (Docker)

```bash
docker build -t app .
docker run -p 3000:3000 app
docker-compose up
```

````

---

## Настройки среды разработки

### Операционная система

```markdown
### Среда разработки
- Операционная система: **Windows 10/11**
- Использовать Windows-команды и пути с обратными слешами (\)
- При поиске файлов и текста использовать Windows-совместимые команды
- Учитывать особенности PowerShell/Command Prompt
````

### Инструменты разработки

```markdown
### Инструменты

- Редактор: VS Code / WebStorm / Vim
- Терминал: PowerShell / Bash / Zsh
- Менеджер пакетов: npm / pnpm / yarn
- Контроль версий: Git
- Контейнеризация: Docker
- CI/CD: GitHub Actions / Jenkins
```

### Переменные окружения

```markdown
### Переменные окружения

- NODE_ENV: development / production
- API_BASE_URL: http://localhost:3000
- DATABASE_URL: postgresql://...
- LOG_LEVEL: debug / info / warn / error
```

---

## Кодовые соглашения

### Именование идентификаторов

#### Базовые правила

```markdown
### Конвенции именования

#### Переменные и функции

- JavaScript/TypeScript: camelCase
- Python: snake_case
- Go: camelCase (экспортируемые PascalCase)
- Rust: snake_case

#### Константы

- Все языки: SCREAMING_SNAKE_CASE

#### Классы и типы

- Все языки: PascalCase

#### Файлы

- Компоненты: PascalCase.tsx
- Утилиты: kebab-case.js
- Тесты: filename.test.js / filename.spec.js
```

#### Специфичные правила проекта

````markdown
### Идентификаторы

- **В структурах данных (JSON/интерфейсы):**

  - Уникальный идентификатор: `id`
  - Ссылка на другую сущность: `id_сущность` (например: `id_country`, `id_city`)

- **В коде (переменные/параметры):**
  - Соответствующие переменные: `idСущность` в camelCase (например: `idCountry`, `idCity`)

Пример:

```typescript
// В интерфейсе
interface i_city {
  id: string;
  id_country: string;
}

// В функциях
function getCity(idCity: string, idCountry: string) { ... }
```
````

````

### Git соглашения

```markdown
### Создание описания коммитов

- **ВСЕГДА анализировать staged файлы** перед созданием описания коммита
- Использовать `git status` для проверки staged файлов
- Если нет staged файлов - сообщить пользователю о необходимости добавить файлы через `git add`
- Анализировать изменения через `git diff --staged`
- Создавать описание коммита на основе реальных изменений в staged файлах

#### Формат сообщений коммитов
- Commit сообщения генерировать **полностью на русском языке** в повелительном наклонении
- Действие указывать на русском (например: "добавить", "исправить", "обновить", НЕ "feat:", "fix:", "docs:")
- Максимум 50 символов для первой строки
- Заголовок с заглавной буквы, без точки в конце
- Затем пустая строка и развёрнутое описание в виде списка
- В теле коммита описывать ЧТО именно было изменено и ПОЧЕМУ, не описывать КАК

#### Примеры коммитов
- "Добавить кэширование погоды"
- "Исправить ошибку загрузки изображений"
- "Обновить зависимости проекта"
````

---

## Технологический стек

### Описание технологий

```markdown
## Технологии

### Фронтенд

- React 18 + TypeScript
- Vite (сборщик)
- Electron (десктопное приложение)
- CSS Modules / Styled Components / Tailwind

### Бэкенд

- Node.js + Express / Fastify
- Python + FastAPI / Django
- Go + Gin / Echo

### База данных

- PostgreSQL / MySQL / MongoDB
- Redis (кэширование)
- SQLite (разработка)

### Инструменты

- ESLint + Prettier (JavaScript/TypeScript)
- Black + isort (Python)
- gofmt (Go)
- Cargo fmt (Rust)
```

### Конфигурация инструментов

````markdown
### Настройки инструментов

#### ESLint

```json
{
	"extends": ["@typescript-eslint/recommended"],
	"rules": {
		"no-unused-vars": "error",
		"prefer-const": "error"
	}
}
```
````

#### Prettier

```json
{
	"semi": true,
	"singleQuote": true,
	"tabWidth": 2,
	"trailingComma": "es5"
}
```

#### TypeScript

```json
{
	"strict": true,
	"noImplicitAny": true,
	"strictNullChecks": true
}
```

````

---

## Тестирование

### Настройки тестирования

```markdown
### Тестирование: Vitest

При создании тестов использовать **Vitest**:

- Файлы тестов: `*.test.ts`, `*.spec.ts`
- Команда запуска: `pnpm test`
- Поддержка мокинга через `vi.mock()`, `vi.fn()`, `vi.spyOn()`

#### Структура тестов
- Unit тесты: `src/**/*.test.ts`
- Integration тесты: `tests/integration/`
- E2E тесты: `tests/e2e/`

#### Покрытие кода
- Минимальное покрытие: 80%
- Команда: `pnpm test:coverage`
- Исключения: конфигурационные файлы, типы
````

### Другие фреймворки

````markdown
### Jest (альтернатива)

```json
{
	"testEnvironment": "jsdom",
	"setupFilesAfterEnv": ["<rootDir>/src/setupTests.ts"],
	"testMatch": ["**/__tests__/**/*.ts", "**/*.test.ts"]
}
```
````

### Pytest (Python)

```ini
[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --cov=src --cov-report=html
```

### Cypress (E2E)

```json
{
	"baseUrl": "http://localhost:3000",
	"viewportWidth": 1280,
	"viewportHeight": 720
}
```

````

---

## Структура проекта

### Организация директорий

```markdown
## Структура проекта

### Основные директории
- `/src/` - исходный код приложения
- `/tests/` - тесты (если отдельно от src)
- `/docs/` - документация
- `/build/` или `/dist/` - собранные файлы
- `/public/` - статические ресурсы
- `/config/` - конфигурационные файлы

### Специфичные директории
- `/resources/` - ресурсы приложения (Electron)
- `/locales/` - файлы локализации
- `/data/` - данные приложения
- `/cache/` - кэш приложения
- `/assets/` - изображения, шрифты
- `/components/` - React компоненты
- `/utils/` - утилиты и хелперы
- `/hooks/` - React хуки
- `/types/` - TypeScript типы
````

### Файловая архитектура

```markdown
### Архитектура файлов

#### Feature-based (рекомендуется)
```

src/
├── features/
│ ├── auth/
│ │ ├── components/
│ │ ├── hooks/
│ │ ├── services/
│ │ └── types/
│ └── dashboard/
├── shared/
│ ├── components/
│ ├── utils/
│ └── types/
└── app/
├── store/
└── router/

```

#### Layer-based (традиционный)
```

src/
├── components/
├── hooks/
├── services/
├── utils/
├── types/
└── pages/

```

```

---

## Продвинутые настройки

### Условные правила

```markdown
### Условные настройки

#### Для разных типов файлов

- **.ts/.js файлы**: строгий линтинг, обязательные типы
- **.test.ts файлы**: более мягкие правила, разрешены any
- **.config.js файлы**: отключить некоторые правила ESLint
- **.d.ts файлы**: только объявления типов

#### Для разных окружений

- **development**: подробные логи, hot reload
- **production**: минификация, оптимизация
- **test**: моки, изоляция
```

### Интеграции

````markdown
### Интеграции

#### CI/CD

```yaml
# .github/workflows/test.yml
name: Test
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      - run: npm ci
      - run: npm test
      - run: npm run build
```
````

#### Pre-commit hooks

```json
{
	"husky": {
		"hooks": {
			"pre-commit": "lint-staged",
			"commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
		}
	},
	"lint-staged": {
		"*.{js,ts,tsx}": ["eslint --fix", "prettier --write"]
	}
}
```

````

### Monitoring и логи

```markdown
### Мониторинг

#### Логирование
- Уровни: error, warn, info, debug
- Формат: JSON в production, human-readable в development
- Не логировать sensitive данные

#### Метрики
- Performance timing
- Error tracking
- User analytics (с согласием)
````

---

## Лучшие практики

### Создание эффективного AGENT.md

#### Принципы качественного файла

1. **Конкретность**: Четкие и недвусмысленные инструкции
2. **Полнота**: Покрытие всех важных аспектов проекта
3. **Актуальность**: Регулярное обновление при изменениях
4. **Краткость**: Избегание избыточной информации
5. **Примеры**: Иллюстрация правил конкретными случаями

#### Структурирование информации

```markdown
### Рекомендуемая структура

1. **Заголовок и описание** - что это за проект
2. **Правила работы** - как должен вести себя ИИ
3. **Команды** - что нужно знать для разработки
4. **Технологии** - какой стек используется
5. **Конвенции** - стандарты кодирования
6. **Структура** - организация проекта
7. **Особенности** - специфические требования
```

#### Типичные ошибки

❌ **Избегать:**

- Слишком общие формулировки
- Устаревшая информация
- Противоречивые правила
- Избыточные детали
- Отсутствие примеров

✅ **Делать:**

- Конкретные инструкции
- Актуальные команды
- Согласованные правила
- Необходимый минимум
- Практические примеры

### Поддержка файла

```markdown
### Поддержка AGENT.md

#### Регулярное обновление

- При изменении технологий
- При обновлении зависимостей
- При изменении процессов
- При добавлении новых команд

#### Командная работа

- Обсуждение изменений в команде
- Документирование решений
- Консистентность между проектами
- Обучение новых участников
```

### Примеры для разных типов проектов

#### React приложение

```markdown
# AGENT.md - React App

## Технологии

- React 18 + TypeScript
- Vite + SWC
- React Router v6
- React Query
- Tailwind CSS

## Команды

- `pnpm dev` - разработка
- `pnpm build` - сборка
- `pnpm test` - тесты с Vitest
- `pnpm lint` - ESLint + Prettier

## Правила

- Функциональные компоненты + хуки
- TypeScript строгий режим
- CSS через Tailwind классы
- Состояние через React Query + Zustand
```

#### Node.js API

```markdown
# AGENT.md - Node.js API

## Технологии

- Node.js 18 + TypeScript
- Fastify фреймворк
- Prisma ORM + PostgreSQL
- Jest для тестирования

## Команды

- `pnpm dev` - разработка с nodemon
- `pnpm build` - TypeScript компиляция
- `pnpm test` - Jest тесты
- `pnpm db:migrate` - миграции БД

## Правила

- RESTful API дизайн
- Валидация через Zod
- Логирование через Pino
- Аутентификация JWT
```

#### Python проект

```markdown
# AGENT.md - Python App

## Технологии

- Python 3.11
- FastAPI + Pydantic
- SQLAlchemy + Alembic
- Pytest + Coverage

## Команды

- `python -m app` - запуск
- `pytest` - тесты
- `black .` - форматирование
- `mypy .` - проверка типов

## Правила

- Type hints обязательно
- Black + isort форматирование
- Docstrings в Google стиле
- 90%+ покрытие тестами
```

---

## Заключение

**AGENT.md файл** является ключевым инструментом для настройки поведения AI помощника Amp в контексте конкретного проекта. Правильно настроенный файл:

1. **Повышает эффективность** работы с ИИ
2. **Стандартизирует подходы** в команде
3. **Сокращает время** на объяснения контекста
4. **Обеспечивает консистентность** кода и процессов
5. **Упрощает onboarding** новых участников

### Ключевые принципы

- **Специфичность**: Четкие и конкретные инструкции
- **Актуальность**: Регулярные обновления
- **Полнота**: Покрытие всех важных аспектов
- **Практичность**: Фокус на реальных потребностях проекта

Хорошо составленный AGENT.md становится живой документацией проекта, которая помогает как людям, так и ИИ лучше понимать контекст и требования разработки.
