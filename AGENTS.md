# AGENTS.md - Настройки для AI помощников

## Структура docs

- `docs/common/` — общие правила, которые можно переиспользовать между проектами (например: `naming-conventions.md`, `git-commit-description.md`, `dev-environment.md`).

- `docs/project/` — настройки, специфичные для этого проекта (например: `project-structure.md`, `tech-stack.md`, `testing-guidelines.md`).

## Источник правил

- **Все правила для AI‑помощников в рамках этого репозитория задаются в этом файле (`AGENTS.md`).**
- Личные `User Rules` пользователя не должны противоречить этому файлу.
- Если для другого проекта нужны другие правила, они должны быть оформлены в его собственном `AGENTS.md`.

## Правила работы

### Язык общения

- **ВСЕГДА отвечать на русском языке**, кроме случаев, когда пользователь явно просит ответить на другом языке

- Объяснять логику кода на русском языке

- Комментарии к коду писать на русском

- Названия переменных/функций оставлять на английском

- Сообщения об ошибках переводить и объяснять на русском

- Использовать примеры кода с русскими комментариями

- При рефакторинге объяснять причины изменений на русском

### Использование MCP и Context7

- Always use context7 when I need code generation, setup or configuration steps, or library/API documentation.
- Context7 следует использовать автоматически, без дополнительного запроса от пользователя, когда:
  - требуется генерация кода;
  - нужны шаги по настройке/конфигурации;
  - необходима документация по библиотекам или API.
- При необходимости документации по библиотеке:
  - сначала через Context7 определить идентификатор библиотеки;
  - затем получить её документацию/справку через MCP‑инструменты.

### Логика рассуждений (sequential thinking)

- When the user explicitly writes "use sequential-thinking", the assistant must follow a structured, step-by-step reasoning and planning process before generating or editing code, and clearly reflect this process in its response.
- Дополнительно, при сложных задачах (архитектура, нетривиальные решения, несколько возможных подходов) ассистент:
  - применяет последовательное, структурированное рассуждение даже без явного ключевого слова;
  - сначала кратко формулирует план шагов, затем действует по этому плану.

### Работа с командной строкой и ОС

- Перед выполнением консольных команд ассистент обязан:
  - учитывать указанную в контексте операционную систему и shell (например, Windows + PowerShell);
  - подбирать команды и синтаксис, совместимые с этой средой.
- Если команда кроссплатформенная, но реализуется по‑разному (например, в `bash` и `powershell`), ассистент:
  - либо даёт адаптированный вариант для текущей ОС;
  - либо явно указывает различия и нужный вариант команды.

### Подтверждение изменений

- **ВСЕГДА показывать план изменений и спрашивать подтверждение перед edit_file/create_file**

- Описать что будет изменено/создано перед применением

- Дождаться явного подтверждения от пользователя

- Перед использованием инструментов редактирования файлов (`ApplyPatch`, `Write` и т.п.) также требуется показать план изменений и дождаться явного подтверждения.

- Без подтверждения разрешается только:
  - читать файлы;
  - предлагать изменения в виде текста/патча в ответе, но не применять их к файлам.

### 🎯 Правила вывода и редактирования

- Вывод кода в чат: `docs/common/agent-output-rules.md`.
- Внесение правок и сохранение комментариев: `docs/common/agent-editing-rules.md`.
- JSDoc и текстовые пояснения: `docs/common/agent-docstyle-rules.md`.

### Создание описания коммитов

Полное описание: `docs/common/git-commit-description.md`.

### Технологии

Полное описание: `docs/project/tech-stack.md`.

### Настройки тестирования

Полное описание: `docs/project/testing-guidelines.md`.

### Среда разработки

Полное описание: `docs/common/dev-environment.md`.

## Команды проекта

### Конвенции именования

Полное описание: `docs/common/naming-conventions.md`.

### Структура проекта

Полное описание: `docs/project/project-structure.md`.

### Разработка

Полное описание: `docs/project/run-and-build.md`.
